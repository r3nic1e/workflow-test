name: Run on created tag

on:
  create:
    tags: ['*']

jobs:
  tag-push:
    runs-on: ubuntu-latest
    steps:
      - name: debug
        env:
          EVENT: ${{ toJSON(github.event) }}
        run: echo "$EVENT"

      - run: echo $GITHUB_REF
